import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-concap-map',
  imports: [],
  template: `<p>concapMap works!</p>`,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ConcapMapComponent { }
